# Server Configuration
PORT=3001
JWT_SECRET=your_secret_key_change_me
GEMINI_API_KEY=your_google_gemini_api_key

# Database Connection (Default: Local MongoDB)
# Only change if you are using a cloud cluster or different port
DATABASE_URL=mongodb://localhost:27017

# Only if using Prisma with SQL (Postgres/MySQL) - not used for current Mongo setup
# DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"